{
  "name": "p2p-config-sync",
  "version": "1.0.0",
  "description": "P2P JSON Config Sync - Full Stack Application",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:node1": "npm run dev --prefix backend",
    "dev:node2": "npm run node2 --prefix backend",
    "start:frontend": "npm start --prefix frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
